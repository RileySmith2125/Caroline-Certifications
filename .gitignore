# Local PDF (don't commit copyrighted content)
*.pdf

# Parser intermediates (large, regeneratable)
parser/_pages/
parser/_pages_small/
parser/_text/
parser/_manifest*.json
parser/_draft_questions*.json
parser/_batch_*.txt
parser/_batch_*_result.json
parser/_matching_batches.json
parser/_visual_batch_*.json
parser/_visual_queue_*.json
parser/_visual_questions_*.json
parser/_tmp_*
parser/_crops*
parser/__pycache__/
# Temporary crops agents create at the project root while doing visual extraction.
_tmp_*.png

# Extracted images aren't (yet) referenced from exam.json — every question has
# stem_images: []. Excluding to keep the repo small. If we later attach images
# to specific questions, commit those individual files explicitly.
data/*/img/

# Claude Code local settings
.claude/

# OS
.DS_Store
Thumbs.db
